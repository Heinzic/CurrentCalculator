from app.internal.admin import (
    calculating_admin,
    consumer_admin,
    consumer_type_admin,
    input_power_admin,
    object_admin,
    section_admin,
    user_admin,
)
from django.contrib import admin

admin.site.site_title = "Backend course"
admin.site.site_header = "Backend course"
